# Quiz: Lesson 3

Name: ____________________

Choose the best answer for each question.

1. What is a condition?
   - Code that stores text
   - An expression that returns `true` or `false`
   - A variable that can hold any type
   - A loop that always runs 5 times


2. Which operator means "greater than or equal to"?
   - `=>`
   - `>=`
   - `==`
   - `<=`


3. If `int age = 14;`, what value does the following expression produce?
   ```
   age >= 13
   ```

   - `14`
   - `"true"`
   - `true`
   - `13`


4. What does an `if` statement do?
   - Repeats code forever
   - Lets the program choose what to do based on a condition
   - Makes your computer stop and thing really hard
   - Prints input to the screen


5. What does `&&` mean in a condition?
   - Both conditions must be true
   - At least one condition must be true
   - The two sides must be equal
   - Repeat the code again


6. What does this code do?
   ```
   int count = 1;

   while (count <= 3) {
       System.out.println(count);
       count = count + 1;
   }
   ```

   - Repeats until `count` is no longer less than or equal to 3
   - Repeats until `count` becomes exactly 3
   - Repeats forever because `count` never changes
   - Runs only if `count` starts at 0
