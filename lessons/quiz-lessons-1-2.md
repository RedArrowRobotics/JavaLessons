# Quiz: Lessons 1 and 2

Name: ____________________

Choose the best answer for each question.

1. What is an IDE?
   - A kind of variable
   - A tool that helps you write and run programs
   - A Java type for text
   - A robot that does your homework


2. Match each type to what it usually holds:

      | `int`      | text               |
      |------------|--------------------|
      | `double`   | **whole number**   |
      | `String`   | **true or false**  |
      | `boolean`  | **decimal number** |


3. Which Java line correctly defines a variable called `age`?

   - `age int = 12;`
   - `int = age 12;`
   - `int age = 12;`
   - `12 = int age;`
   

4. Why do most Java lines end with a semicolon (`;`)?
   - It tells Java the instruction is finished
   - It means the line is a comment
   - It makes the variable a String
   - It tells Java to print the line
 

5. Which line prints the value of the variable `name`?
   - `print(name);`
   - `System.out.println(name);`
   - `System.println.out(name);`
   - `println.System.out(name);`


6. If `String one = "My name is ";` and `String two = "Dave";`, what does `System.out.println(one + two);` print?
   - `one + two`
   - `My name is Dave`
   - `DaveMy name is`
   - `My name is + Dave`


.

.

7. What does `input.nextLine()` do?
   - Prints a line to the screen
   - Deletes the last line
   - Waits for the user to type something and press Enter
   - Changes a number into a String


8. If `age` is an `int`, would this line work?
    ```
    age = "fourteen";
    ```

    - Yes, because Java can change the type of a variable
    - Yes, because `"fourteen"` and `14` mean the same thing
    - No, because an `int` variable can only hold whole numbers
    - No, because strings always need `System.out.println`


9. What would this code print?

    ```
    int age = 12;
    System.out.println(age);
    age = 13;
    System.out.println(age);
    ```

    - '12' then '13'
    - '13' then '12'
    - '12' then '12'
    - Nothing


10. What is input?
    - Data the user gives to the program
    - Data the program prints to the screen
    - A special kind of comment
    - When you yell at the computer and hope it listens

